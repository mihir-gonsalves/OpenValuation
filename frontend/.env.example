# Base URL of the OpenValuation API.
# Leave EMPTY in development: Vite proxies /api -> http://localhost:8000.
# In production (Vercel), set this to the Render backend URL, e.g.
# VITE_API_BASE=https://openvaluation-api.onrender.com

VITE_API_BASE=